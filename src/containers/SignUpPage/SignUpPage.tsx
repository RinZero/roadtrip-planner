import React, { memo } from 'react'

import { Typography } from '@material-ui/core'

const SignUpPage = () => {
  return <Typography variant="h1">Anmeldung</Typography>
}

export default memo(SignUpPage)
